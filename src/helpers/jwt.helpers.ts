// import jwt, { Secret } from 'jsonwebtoken';
// const createToken = (
//   payload: object,
//   secret: Secret,
//   expireTime: string
// ): string => {
//   return jwt.sign(payload, secret, expireIn:expireTime);
// };

// export const jwtHelpers = {
//   createToken,
// };
